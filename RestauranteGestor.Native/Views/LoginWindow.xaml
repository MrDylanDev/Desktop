<Window x:Class="RestauranteGestor.Native.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RestoOS - Ingreso" Width="460" Height="420" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Background="{StaticResource Surface}" Foreground="{StaticResource OnSurface}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,12,0,0">
      <TextBlock Text="◉  RestoOS" Foreground="{StaticResource Primary}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
      <TextBlock Text="Selecciona tu perfil" FontSize="13" Foreground="{StaticResource OnSurfaceVariant}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Margin="0,20,0,0">
      <TextBlock Text="Ingresar como" FontSize="12" Foreground="{StaticResource OnSurfaceVariant}" FontWeight="Bold" HorizontalAlignment="Center" Opacity="0.7"/>
    </StackPanel>

    <StackPanel Grid.Row="2" VerticalAlignment="Center" Margin="0,20,0,0">
      <Button Tag="Admin" Click="Role_Click" Style="{StaticResource PrimaryButton}" Height="96" Margin="0,0,0,16" HorizontalContentAlignment="Stretch">
        <Grid>
          <Grid.ColumnDefinitions><ColumnDefinition Width="48"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <TextBlock Text="◆" FontSize="28" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#5D1800"/>
          <StackPanel Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center">
            <TextBlock Text="Administrador" FontWeight="Bold" FontSize="16" Foreground="#5D1800"/>
            <TextBlock Text="Solo Administración: Módulos y Reportes" FontSize="11" Foreground="#5D1800" Opacity="0.85" TextWrapping="Wrap"/>
          </StackPanel>
          <TextBlock Grid.Column="2" Text="›" FontSize="20" Foreground="#5D1800" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </Grid>
      </Button>

      <Button Tag="Empleado" Click="Role_Click" Style="{StaticResource SecondaryButton}" Height="96" Margin="0,0,0,0" HorizontalContentAlignment="Stretch">
        <Grid>
          <Grid.ColumnDefinitions><ColumnDefinition Width="48"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <TextBlock Text="▦" FontSize="26" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource OnSurface}"/>
          <StackPanel Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center">
            <TextBlock Text="Empleados" FontWeight="Bold" FontSize="16"/>
            <TextBlock Text="Operación: POS, Mesas, KDS, Inventario, Reservas, Menú" FontSize="11" Foreground="{StaticResource OnSurfaceVariant}" TextWrapping="Wrap"/>
          </StackPanel>
          <TextBlock Grid.Column="2" Text="›" FontSize="20" Foreground="{StaticResource OnSurfaceVariant}" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </Grid>
      </Button>

    </StackPanel>
  </Grid>
</Window>
